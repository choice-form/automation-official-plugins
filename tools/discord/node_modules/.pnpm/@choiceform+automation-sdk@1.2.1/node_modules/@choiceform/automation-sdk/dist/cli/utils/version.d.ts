/**
 * 动态读取 SDK 版本号
 * @returns SDK 版本号
 */
export declare function getSdkVersion(): Promise<string>;
/**
 * 获取带版本前缀的 SDK 版本（用于依赖）
 * @param prefix 版本前缀，默认为 '^'
 * @returns 带前缀的版本号，如 '^1.0.2'
 */
export declare function getSdkVersionWithPrefix(prefix?: string): Promise<string>;
/**
 * 获取 SDK 包名
 * @returns SDK 包名
 */
export declare function getSdkPackageName(): string;
/**
 * 检查版本兼容性
 * @param userVersion 用户使用的版本
 * @param currentVersion 当前 SDK 版本
 * @returns 是否兼容
 */
export declare function isVersionCompatible(userVersion: string, currentVersion: string): boolean;
